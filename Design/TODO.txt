
Renovate Component system
	. Replace static GetName in components to be virtual GetTag()
	. Instead of assuming the GetName() exists, GameObject now creates an instance of the TYPE and uses its overloaded GetTag().
	. New functionality allows for accessing base Components, stored in a GameObject, from derived Components
	. Simplified Component management
. make PlayerCollider inhert BoxCollider



. Create LIBRARYNAME.h
. In the future, after all else fails, Colliders will return an approximation of the collision.

. make PlayerCollider that prevents him from falling through the world


. Allow user to select tools using numbers on the keyboard
. Create text display in GP2D
. Create GUI input, slider, text display in GP2D



. Make Scene objects that contain a registry of GameObjects. This will allow you to build worlds that contain NPCs, Tilemap(s), Player(s), Items, story elements, GUI, and other objects.


. Sand tiles are updated in the wrong order. This causes them to float in space even if the bottom-most tile can move and make space. They should be updated from the bottom up.

. Create Tilemap::Raycast()
. Experiment with DDA raycasting
. Use quadtree library

