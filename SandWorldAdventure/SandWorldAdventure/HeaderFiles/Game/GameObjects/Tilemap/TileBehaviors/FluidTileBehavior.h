#pragma once
#include "TileBehavior.h"

namespace SandboxEngine::Game::GameObject::Tilemap
{
	/*class FluidTileBehavior : public TileBehavior
	{
	public:
		static void TryMoveFluidTile(std::map<Vector2, bool>* pUpdateQueue, SandboxEngine::Game::Tilemap::Tilemap* pTilemap, Tile* pTile, Vector2 tilePosition, Vector2 appliedMotion, double currentTime);

		virtual void ApplyTilePhysics(SandboxEngine::Game::Tilemap::Tilemap* pTilemap, Vector2 tilePosition, Vector2 chunkPosition, Tile* pTile, Chunk* pChunk, std::map<Vector2, bool>* pNewUpdateQueue, double currentTime) override;
	};*/
}